<UserControl
    x:Class="MaiziWPF.Core.DateRangeControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="MaterialCalendarStyle" TargetType="Calendar">
            <Setter Property="Foreground" Value="#212121" />
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#E0E0E0" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CalendarDayButtonStyle">
                <Setter.Value>
                    <Style TargetType="CalendarDayButton">
                        <Setter Property="MinWidth" Value="36" />
                        <Setter Property="MinHeight" Value="36" />
                        <Setter Property="FontSize" Value="10" />
                        <Setter Property="FontWeight" Value="Normal" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="CalendarDayButton">
                                    <Grid>
                                        <Border
                                            x:Name="Border"
                                            Background="Transparent"
                                            BorderBrush="Transparent"
                                            CornerRadius="4" />
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsToday" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#FFEBF5FF" />
                                            <Setter TargetName="Border" Property="BorderBrush" Value="#2196F3" />
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#2196F3" />
                                            <Setter Property="Foreground" Value="White" />
                                        </Trigger>
                                        <Trigger Property="IsBlackedOut" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#FFE7E5E5" />
                                            <Setter TargetName="Border" Property="BorderBrush" Value="#FFE7E5E5" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBox
            x:Name="StartDateText"
            HorizontalContentAlignment="Center"
            materialDesign:HintAssist.Hint="开始时间"
            materialDesign:TextFieldAssist.DecorationVisibility="Hidden"
            BorderThickness="0"
            FontSize="16" />
        <Label Grid.Column="1" Content="-" />
        <TextBox
            x:Name="EndDateText"
            Grid.Column="2"
            HorizontalContentAlignment="Center"
            materialDesign:HintAssist.Hint="结束时间"
            materialDesign:TextFieldAssist.DecorationVisibility="Hidden"
            materialDesign:TextFieldAssist.HasClearButton="True"
            BorderThickness="0"
            FontSize="16" />
        <Button
            Grid.Column="3"
            Padding="0"
            Style="{StaticResource MaterialDesignToolButton}">
            <materialDesign:PopupBox
                x:Name="DateRangePopupBox"
                Padding="0"
                PlacementMode="BottomAndAlignRightEdges"
                StaysOpen="True">
                <materialDesign:PopupBox.ToggleContent>
                    <materialDesign:PackIcon Kind="DateRange" Opacity=".56" />
                </materialDesign:PopupBox.ToggleContent>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" SharedSizeGroup="CalendarCol" />
                        <ColumnDefinition Width="Auto" SharedSizeGroup="CalendarCol" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" SharedSizeGroup="CalendarCol" />
                        <RowDefinition Height="Auto" SharedSizeGroup="CalendarCol" />
                    </Grid.RowDefinitions>
                    <Calendar
                        x:Name="StartDate"
                        Grid.Column="0"
                        Margin="0,-2,0,0"
                        Style="{StaticResource MaterialCalendarStyle}" />
                    <Calendar
                        x:Name="EndDate"
                        Grid.Column="1"
                        Margin="0,-2,0,0"
                        Style="{StaticResource MaterialCalendarStyle}" />
                    <StackPanel
                        Grid.Row="1"
                        Grid.Column="1"
                        Margin="0,0,4,4"
                        HorizontalAlignment="Right"
                        Orientation="Horizontal">
                        <Button
                            x:Name="ConfirmDateRange"
                            Content="确定"
                            Style="{StaticResource MaterialDesignFlatButton}" />
                        <Button
                            x:Name="CancelDateRange"
                            Content="取消"
                            Foreground="#9e9d9d"
                            Style="{StaticResource MaterialDesignFlatButton}" />
                    </StackPanel>
                </Grid>
            </materialDesign:PopupBox>
        </Button>
    </Grid>
</UserControl>
